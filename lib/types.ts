import type { ChatInputCommandJSON } from '@amethyst/ddframework/lib/types.ts';

/**
 * Re-exports the core ChatInputCommandJSON type for external consumers.
 *
 * This allows consumers to import the canonical command schema type from a single location.
 */
export type { ChatInputCommandJSON };
